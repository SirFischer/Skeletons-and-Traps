#include "Camera.hpp"

Camera::Camera()
{
	sf::View mView;
	sf::Window window;
	/*mView.setViewport(sf::FloatRect(0.25f, 0.25, 0.5f, 0.5f));
	mSprite.setTexture(*ResourceManager::LoadTexture("assets/textures/Hero.png"));
	mPosition = mSprite.getPosition();
	mView.setCenter(mPosition);*/

}

Camera::~Camera()
{
}

